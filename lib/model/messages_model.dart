import 'package:flutter/material.dart';

class MessagesModel {
  Color roundColor;
  String title;
  String voices;
  String image;
  Color textColor;
  String hour;

  MessagesModel(this.roundColor, this.title, this.voices, this.image,
      this.textColor, this.hour);
}
